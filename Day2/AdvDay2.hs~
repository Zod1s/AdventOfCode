import System.IO

main :: IO()
main = do
  handle <- openFile "input.txt" ReadMode 
  contents <- hGetContents handle
  let passwords = lines contents
  print $ length $ validPass passwords

validPass :: [String] -> [String]
validPass = undefined
